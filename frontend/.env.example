# Leave unset when the API is served from the same origin (Vite dev proxy or
# the nginx reverse proxy in docker-compose). The app defaults to /api/v1.
# Set this only for split-origin deployments.
# VITE_API_BASE_URL=https://api.example.com/api/v1
